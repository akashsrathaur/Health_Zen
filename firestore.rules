
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary: Allow authenticated users to read and write any document
    // This helps debug authentication vs authorization issues
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
